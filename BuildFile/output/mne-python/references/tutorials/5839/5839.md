# How To: 5839

**Difficulty**: Advanced
**Estimated Time**: 20 minutes
**Tags**: workflow, integration

## Overview

Workflow: Test concatenating raw objects with annotations.

## Prerequisites

**Required Modules:**
- `math`
- `os`
- `re`
- `contextlib`
- `io`
- `os`
- `pathlib`
- `numpy`
- `pytest`
- `numpy.testing`
- `mne`
- `mne`
- `mne._fiff._digitization`
- `mne._fiff.constants`
- `mne._fiff.meas_info`
- `mne._fiff.pick`
- `mne._fiff.proj`
- `mne._fiff.utils`
- `mne.io`
- `mne.io.base`
- `mne.transforms`
- `mne.utils`


## Step-by-Step Guide

### Step 1: 'Test concatenating raw objects with annotations.'

```python
'Test concatenating raw objects with annotations.'
```

**Verification:**
```python
assert_array_equal(raw_A.annotations.onset, EXPECTED_ONSET)
```

### Step 2: Assign EXPECTED_ONSET = value

```python
EXPECTED_ONSET = [1.5, 2.0, 2.0, 2.5]
```

**Verification:**
```python
assert_array_equal(raw_A.annotations.duration, EXPECTED_DURATION)
```

### Step 3: Assign EXPECTED_DURATION = value

```python
EXPECTED_DURATION = [0.2, 0.0, 0.0, 0.2]
```

**Verification:**
```python
assert_array_equal(raw_A.annotations.description, EXPECTED_DESCRIPTION)
```

### Step 4: Assign EXPECTED_DESCRIPTION = value

```python
EXPECTED_DESCRIPTION = ['dummy', 'BAD boundary', 'EDGE boundary', 'dummy']
```

**Verification:**
```python
assert raw_A.annotations.orig_time == _stamp_to_dt((0, 0))
```

### Step 5: Assign unknown = value

```python
raw_A, raw_B = (raw_factory((x, 0)) for x in [0, 2])
```

### Step 6: Call raw_A.append()

```python
raw_A.append(raw_B)
```

### Step 7: Call assert_array_equal()

```python
assert_array_equal(raw_A.annotations.onset, EXPECTED_ONSET)
```

### Step 8: Call assert_array_equal()

```python
assert_array_equal(raw_A.annotations.duration, EXPECTED_DURATION)
```

### Step 9: Call assert_array_equal()

```python
assert_array_equal(raw_A.annotations.description, EXPECTED_DESCRIPTION)
```

**Verification:**
```python
assert raw_A.annotations.orig_time == _stamp_to_dt((0, 0))
```

### Step 10: Assign raw = RawArray(...)

```python
raw = RawArray(data=np.empty((10, 10)), info=create_info(ch_names=10, sfreq=10.0), first_samp=10)
```

### Step 11: Call raw.set_meas_date()

```python
raw.set_meas_date(meas_date)
```

### Step 12: Call raw.set_annotations()

```python
raw.set_annotations(annotations=Annotations(onset=[0.5], duration=[0.2], description='dummy', orig_time=None))
```


## Complete Example

```python
# Workflow
'Test concatenating raw objects with annotations.'
EXPECTED_ONSET = [1.5, 2.0, 2.0, 2.5]
EXPECTED_DURATION = [0.2, 0.0, 0.0, 0.2]
EXPECTED_DESCRIPTION = ['dummy', 'BAD boundary', 'EDGE boundary', 'dummy']

def raw_factory(meas_date):
    raw = RawArray(data=np.empty((10, 10)), info=create_info(ch_names=10, sfreq=10.0), first_samp=10)
    raw.set_meas_date(meas_date)
    raw.set_annotations(annotations=Annotations(onset=[0.5], duration=[0.2], description='dummy', orig_time=None))
    return raw
raw_A, raw_B = (raw_factory((x, 0)) for x in [0, 2])
raw_A.append(raw_B)
assert_array_equal(raw_A.annotations.onset, EXPECTED_ONSET)
assert_array_equal(raw_A.annotations.duration, EXPECTED_DURATION)
assert_array_equal(raw_A.annotations.description, EXPECTED_DESCRIPTION)
assert raw_A.annotations.orig_time == _stamp_to_dt((0, 0))
```

## Next Steps


---

*Source: test_raw.py:742 | Complexity: Advanced | Last updated: 2026-05-18*